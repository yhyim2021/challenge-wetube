mixin videoList(video)
  div 
    //- video(src=`${video.videoUrl}` controls)
    a(href=`/videos/${video._id}/watch`)
      h3=video.title
      if video.owner.username
        small=video.owner.username