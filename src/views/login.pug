extends ./layouts/base 

block content 
  .loginScreen.formScreen
    form(method="POST")
      .formScreen__column 
      .formScreen__column
        input(type="text", name="username", placeholder="Username", required)
        input(type="password", name="password", placeholder="Password", required)
        input(type="submit", value="로그인")